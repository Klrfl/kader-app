---
import BaseLayout from "./BaseLayout.astro";
import NavBar from "@/components/NavBar.astro";

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<BaseLayout title={title}>
  <NavBar />

  <main class="mx-auto max-w-[110rem]">
    <slot />
  </main>
</BaseLayout>
